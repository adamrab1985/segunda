# Database (PostgreSQL) – Neon, Vercel Postgres, etc.
# If using Neon POOLER URL (host has -pooler), add &pgbouncer=true for Vercel
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Same value as X_REPORT_SECRET in Vercel (for Python script to POST)
X_REPORT_SECRET="your-secret-here"

# Backend URL for the script (no trailing slash)
X_REPORT_API_URL="https://your-segunda.vercel.app"
