# Segunda X Report Script

Run from Segunda folder: `python3 scripts/caspit_xreport.py`

Set in Segunda/.env: DATABASE_URL, X_REPORT_SECRET (same as Vercel), X_REPORT_API_URL (your Vercel URL). Optionally TELEGRAM_TOKEN, TELEGRAM_USER_IDS.

Install: `pip install -r requirements.txt`
